/** @purpose Minimal toast container. Replace with full implementation if needed. @owner [Gemini] */
export function Toaster() {
  return <div id="toaster" aria-live="polite" className="fixed bottom-4 right-4 z-50 flex flex-col gap-2" />;
}
